var fact, n, sum;
fact:=5;
n:=1;
sum:=1;
while(n <= fact)
	sum:=sum*n;
	output("fact(",n,") = ", sum);
	n:=n+1;
endwhile

Output(fact, "! is " , sum);
sum:=max(10, 12, 9);
output("max(10, 12, 9):=", sum);
if(sum > 0)
	output("sum is > 0");
endif
